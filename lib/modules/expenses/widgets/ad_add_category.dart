import 'package:flutter/material.dart';

class ADCreateCategory extends StatelessWidget {
  const ADCreateCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
